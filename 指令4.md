我运行 `npm run build` 报错了，提示找不到 `package.json` 文件。
看来我们之前漏掉了这个核心文件。

请帮我生成一个标准的 `package.json` 文件：
1. **Scripts:** 包含 `dev`, `build`, `start`, `lint`。
2. **Dependencies:** 包含 `next`, `react`, `react-dom`, `lucide-react`, `@supabase/supabase-js`, `clsx`, `tailwind-merge`。
3. **DevDependencies:** 包含 `typescript`, `@types/node`, `@types/react`, `tailwindcss`, `postcss`, `autoprefixer`, `eslint`。

请直接给我代码，让我添加到项目里。