Great! The components are generated. Now let's assemble the final page.

Please perform the following **Final Steps**:

1.  **Create `lib/supabase.ts`:**
    - I need the Supabase client helper to fetch data.
    - Please create this file using `@supabase/ssr` (or standard `supabase-js` if simpler for now) so I can query the `platforms` table.

2.  **Create `app/page.tsx` (The Homepage):**
    - This must be a **Server Component**.
    - Fetch data from Supabase: `SELECT * FROM platforms ORDER BY is_sponsored DESC, trust_score DESC`.
    - Render the layout:
      ```tsx
      <main className="min-h-screen bg-neutral-950 text-white">
        <Header />
        <Ticker />
        <Hero />
        <div className="container mx-auto px-4 py-12">
          <PlatformGrid platforms={data} />
        </div>
      </main>
      ```

3.  **Run Dev:**
    - Tell me the command to start the local server to preview the site.

Let's finish the site!